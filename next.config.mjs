/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server Actions are enabled by default in modern Next.js.
  // The old `experimental.serverActions` flag is invalid and can be removed.
};

export default nextConfig;
